<div class="modal fade" id="new-rating-modal">
  <div class="modal-dialog">
    <div class="modal-content" style="text-align: center;">
      <div class="modal-header" style="background-color: #d6009d; color: white; border-top-left-radius: 6px; border-top-right-radius: 6px;">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">New Cleanliness Rating / Note for: <br />
          <%= @restroom.location %></h4>
      </div>
      <div class="modal-body">
        <%= form_for([@restroom, @cleanlinessrating]) do |f| %>
          <%= fields_for @cleanlinessrating do |c| %>
            <div class="field">
              <%= c.label(:cleanlinessrating, "Cleanliness rating, 1 (dirtiest) to 10 (cleanest):") %><br />
              <%= c.select(:cleanlinessrating, 1..10, :selected => 5) %>
            </div>
          <% end %>
          <%= fields_for @note do |n| %>
            <div class="field">
              <%= n.label(:comment, "Notes:") %><br />
              <%= n.text_area :comment, placeholder: "Add your notes about this restroom here", style: "width:50%; height:100px;" %>
            </div>
          <% end %>
          <div class="actions">
            <%= f.submit "Submit"%>
          </div>
        <% end %>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
